{
  "name": "ide-im-skill",
  "version": "0.1.0",
  "description": "Ide-IM-Skill: Bridge IM platforms (Telegram, Discord, Feishu, QQ) to Cursor / Claude / Codex",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "postinstall": "node scripts/patch-upstream.js",
    "typecheck": "tsc --noEmit",
    "dev": "tsx src/main.ts",
    "test": "CTI_HOME=$(mktemp -d) node --test --import tsx --test-timeout=15000 --test-concurrency=1 src/__tests__/*.test.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.62",
    "claude-to-im": "github:op7418/claude-to-im",
    "esbuild": "^0.25.0"
  },
  "optionalDependencies": {
    "@openai/codex-sdk": "^0.110.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.21.0",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20"
  }
}
